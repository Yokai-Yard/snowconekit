import { style } from '@vanilla-extract/css';

export const connectCards = style({
    background: 'rgba(255,255,255,0.9)',
    borderRadius: '8px',
    padding: '10px',
    paddingLeft: '30px',
    marginLeft: '-40px',
});